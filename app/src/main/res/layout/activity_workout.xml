<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:layout_gravity="bottom"
    android:weightSum="100">

    <Button
        android:id="@+id/buttonWorkoutExercices"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_centerHorizontal="true"
        android:layout_weight="33.33"
        android:background="@drawable/exercices"
        android:text="Gestion des Exercices"
        android:textColor="#FFFFFF"
        android:textSize="30sp"
        android:alpha="0.8"/>

    <Button
        android:id="@+id/buttonWorkoutManagment"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_below="@+id/buttonWorkoutExercices"
        android:layout_centerHorizontal="true"
        android:layout_weight="33.33"
        android:background="@drawable/workout"
        android:text="Programme d'entrainement"
        android:textColor="#FFFFFF"
        android:textSize="30sp"
        android:alpha="0.8"/>

</LinearLayout>